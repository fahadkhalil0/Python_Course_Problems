# Can we actually change the self parameter into other parameter?

class self:
    def __init__(self, name):
        self.name = name

    def greet(greet):
        print(f"Hello, {greet.name}!")

Self = self("Fahad Khalil")
Self.greet()

#Yes we can do it.